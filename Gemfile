source 'http://rubygems.org'

gem 'rails'
#gem 'sqlite3-ruby', :require => 'sqlite3' 
gem 'facebook_oauth'      
            
# Mandatory gem for deployment as webbynode rapid stack, but we don't use a database fo
gem 'mysql2'

group :development, :test do
  gem "rspec-rails", ">= 2.0.0" 
  gem "steak" 
  gem "capybara"
end

