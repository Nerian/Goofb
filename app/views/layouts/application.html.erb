<!DOCTYPE html>
<html>
<head>
	<title>Goofb</title>
	<%= stylesheet_link_tag :all %>
	<%= javascript_include_tag :defaults %>
	<%= csrf_meta_tag %>
</head>
<body>
    <div id='wrapper'>
		<div id='header'>
			<ul>
				<li>
					<%= link_to '1. Connect to Facebook', home_index_path%>
					<%= link_to '2. Select the export options', export_new_path%>
				</li>
			</ul>
			   
		</div>
		<div id='content'>
			<%= yield %>      
		</div>
	</div>
</body>
</html>
